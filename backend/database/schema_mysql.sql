CREATE DATABASE IF NOT EXISTS appointment_platform
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE appointment_platform;

-- Laravel migrations are the source of truth. This file is only a phpMyAdmin-friendly overview.
-- Recommended setup: run `php artisan migrate --seed` instead of importing this manually.
